server:
  port: 8080

spring:
  application:
    name: auth-service

  flyway:
    enabled: false

  datasource:
    url: jdbc:mariadb://localhost:3306/auth_db
    username: root
    password: 12345678
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

jwt:
  secret: 9f8d7a6c5b4a3e2f1a9c8b7d6e5f4a3b
  access-token-expiration: 900000
  refresh-token-expiration: 604800000
